# سجل التعديلات — تحديث مذاكر (Reviews + English Deck)

⚠️ **مهم جداً:** الموقع مقسّم فعلياً على عدة ملفات وليس فقط `app.js` و`index.html`
(البنية: `js/app.js`, `js/db.js`, `js/services/*.js`, `js/views/*.js`, `js/utils/helpers.js`,
`css/main.css`). لذلك التحديث يشمل كل الملفات المتأثرة، وليس ملفين فقط. انسخ محتويات
هذا المجلد فوق مجلد مشروعك الحالي بنفس المسارات بالضبط.

---

## 1) إصلاح خوارزمية المراجعة (`js/services/srs.js`)

**قبل:** جدول ثابت من المراحل (Stage 0 → 5) بفواصل جامدة، بدون أي حد للمراجعات اليومية،
فيتسبب بتراكم كل البطاقات المستحقة دفعة واحدة.

**بعد:**
- كل بطاقة أصبحت تملك `ease` (معامل سهولة، يبدأ من 2.5)، `interval` (عدد الأيام)، و`lapses` (عدد الأخطاء).
- ✅ إجابة صحيحة → `interval = interval × ease` (الفترة تتضاعف تصاعدياً حسب سهولة البطاقة).
- ❌ إجابة خاطئة → البطاقة ترجع لليوم التالي فوراً، ويزيد `lapses`، وينخفض `ease` قليلاً.
- دالة جديدة `buildTodayQueue()`:
  1. تجيب كل البطاقات المستحقة.
  2. ترتّبها بالأولوية: **الأكثر أخطاءً أولاً، ثم الأقدم استحقاقاً**.
  3. تاخذ فقط عدد البطاقات المساوي لـ"الحد الأقصى اليومي".
  4. البطاقات الزايدة (overflow) تتأجل تلقائياً على الأيام القادمة (نفس الحد لكل يوم) بدل ما
     تتكدس كلها في يوم واحد — هذا هو الحل الجذري لمشكلة "الانفجار" في عدد البطاقات.
- `getMaxDailyReviews()` / `setMaxDailyReviews()`: تُخزَّن القيمة في الإعدادات، وتُقرأ من مكانين:
  - **مباشرة من صفحة المراجعة نفسها** (مربع صغير أعلى الصفحة، بدون الحاجة للإعدادات) — هذا ما طلبته بالضبط.
  - أيضاً موجودة في صفحة الإعدادات كخيار احتياطي.

## 2) صفحة "المراجعة" (البطاقات) — `js/views/study.js`

- أعيدت كتابتها بالكامل لتستخدم `buildTodayQueue()` الجديدة.
- صندوق "🎚️ الحد اليومي" ظاهر بأعلى الصفحة دايماً، مع رسالة توضح كم بطاقة تأجلت تلقائياً لو تجاوزت الحد.
- فلتر مادة + فلتر تصنيف (قسم) بجانب بعض، ويشمل قسم "إنجليزي".
- زر نطق 🔊 (Web Speech API) يظهر تلقائياً على البطاقات من تصنيف "إنجليزي" (على الوجه والمثال).
- عرض الترجمة + المثال العملي في ظهر البطاقة الإنجليزية إذا كانا موجودين.

## 3) قسم "المراجعات" الجديد كلياً — `js/views/reviews.js` (ملف جديد)

هذا هو الحل لمشكلتك الأساسية: **"المراجعات" أصبح قسم مستقل تماماً عن الفلاش كاردز**،
تماماً بنفس شكل الصورة اللي أرسلتها:

- كل "درس" له بطاقة تحتوي: العنوان، تصنيف (badge)، تاريخ الإنشاء، زر حذف 🗑️.
- **5 مربعات مراجعة** تُجدوَل تلقائياً بعد 1، 3، 7، 21، 40 يوماً من تاريخ الإضافة (تماماً كالصورة).
- ✅ checkbox لكل مراجعة لتحديدها كمكتملة — نسبة الإنجاز وشريط التقدّم يتحدّثان فوراً.
- **تعديل تاريخ أي مراجعة يدوياً** بالضغط مباشرة على التاريخ داخل المربع نفسه (مو من الإعدادات)
  — يمكن تعديله في أي وقت حتى بعد الإضافة أو بعد تحديده كمكتمل.
- حقل "الصفحات / ملاحظات" قابل للتعديل مباشرة (يحفظ تلقائياً).
- **بدون حذف إمكانية إضافة الفلاش كاردز من صفحة المادة** (زي ما كانت بالضبط) — لكن أضفت أيضاً
  زر "🃏 إضافة فلاش كاردز لهذا الدرس" على كل بطاقة درس (يظهر فقط إذا ربطت الدرس بمادة عند
  إنشائه)، فتقدر تضيف بطاقة مباشرة من داخل الدرس دون الذهاب لصفحة المادة.
- تمت إضافة رابط "المراجعات" في القائمة الجانبية (`index.html`) ورابط راوتر `#reviews` في `app.js`.

**ملاحظة معمارية:** خطط المراجعة (`review_plans`) مخزّنة في IndexedDB كمخزن مستقل تماماً عن
مخزن البطاقات (`flashcards`) — بالضبط كما طلبت: "كل قسم منفصل حتى لو تشابهوا".

## 4) قسم اللغة الإنجليزية + الذكاء الاصطناعي

- **`js/db.js`**: أضفت `category` كحقل + index على كل بطاقة (افتراضياً "عام").
- **`js/views/subject-detail.js`**: نافذة إضافة/تعديل البطاقة صار فيها:
  - قائمة اختيار "التصنيف" (عام / إنجليزي).
  - عند اختيار "إنجليزي" تظهر حقول: الترجمة، مثال عملي، وزر
    **"✨ توليد ترجمة ومثال بالذكاء الاصطناعي"** يستخدم مفتاح Gemini المحفوظ عندك في الإعدادات.
  - زر نطق 🔊 على كل بطاقة إنجليزية في قائمة البطاقات.
- **`js/services/gemini.js`**: دالة جديدة `generateWordInsight(apiKey, word)` ترجع
  الترجمة + جملة مثال عملية بالإنجليزي + ترجمتها + نوع الكلمة، بصيغة JSON منظّمة.
- **الصفحة الرئيسية (`dashboard.js`)**: بطاقة إحصائية "🇬🇧 كلمة إنجليزية" + زر
  "🇬🇧 مراجعة الإنجليزية" يفتح مباشرة جلسة مراجعة مفلترة على قسم الإنجليزي فقط
  (بينما تقدر أيضاً تختار "كل الأقسام" من صفحة المراجعة نفسها لمراجعة كل شيء مع بعض).
- من صفحة "المراجعات" (الدروس): إذا صنّفت الدرس كـ"إنجليزي" وضغطت "إضافة فلاش كاردز"،
  تفتح نافذة مخصّصة لإدخال الكلمة + توليد الترجمة والمثال بالذكاء الاصطناعي مباشرة.

## 5) ملفات أخرى تم تعديلها

- **`js/db.js`**: ترقية `DB_VERSION` من 3 إلى 4 (تُنشئ المخزن الجديد + index تلقائياً عند أول تحميل، بدون فقدان أي بيانات قديمة).
- **`js/views/settings.js`**: استبدال قسم "فترات SRS" القديم (اللي ما عاد له معنى) بحقل "الحد الأقصى للمراجعات اليومية".
- **`css/main.css`**: أُضيفت كل تنسيقات القسم الجديد (`review-plan-*`, `max-daily-box`, `category-badge`, `tts-btn`...) في نهاية الملف، متوافقة مع الوضعين الفاتح والداكن.
- **`index.html`**: رابط جديد "المراجعات" في القائمة الجانبية، وتغيير تسمية الرابط القديم إلى "بطاقات المراجعة" للتفريق بين القسمين.

---

## نقاط تحتاج انتباهك

1. **البيانات القديمة محفوظة بالكامل** — الترقية لقاعدة البيانات (v3 → v4) تُضيف فقط المخزن
   والحقول الجديدة، ولا تحذف أي بطاقات أو مواد موجودة.
2. البطاقات القديمة التي لم تُصنَّف من قبل ستُعامل تلقائياً كتصنيف "عام".
3. ميزة توليد الترجمة بالذكاء الاصطناعي تحتاج مفتاح Gemini API محفوظ مسبقاً في الإعدادات (نفس المفتاح المستخدم لتوليد البطاقات من الملفات).
4. زر النطق (TTS) يعتمد على دعم المتصفح لـ Web Speech API — مدعوم في Chrome وEdge بشكل جيد، وقد يختلف الصوت المتاح حسب نظام التشغيل.

---

## تحديث إضافي — تعديلات المراجعات + قسم الإنجليزية المستقل (Lexora)

### 1) صفحة "المراجعات" (`js/views/reviews.js` + `css/main.css`)
- بطاقة الدرس صارت **بعرض الصفحة كامل** بدل الشبكة المزدحمة القديمة.
- زر **"📅 اليوم"** جديد أعلى الصفحة: يفلتر ليريك فقط الدروس التي فيها مراجعة مستحقة اليوم.
- أي مربع مراجعة مستحق اليوم يتلوّن **تلقائياً بالبرتقالي 🔶** (بغض النظر عن الفلتر).
- تمت إزالة حقل "التصنيف" من نافذة إضافة/تعديل الدرس (الإنجليزي له الآن نظامه الخاص بالكامل).
- خوارزمية خطط الدروس بقيت **مستقلة تماماً** عن خوارزمية بطاقات المراجعة (srs.js) — لكل نظام منطقه الخاص، ولا يتأثر أحدهما بالآخر إلا إذا طُلب ربطهما لاحقاً صراحة.

### 2) الشاشة الرئيسية (`js/views/dashboard.js`)
- تعرض الآن **قائمة فعلية** بعناوين الدروس المطلوب مراجعتها اليوم (وليس رقماً فقط)، مع رابط مباشر لكل درس.
- بطاقة إحصائية جديدة لقسم الإنجليزية (سلسلة الأيام 🔥 + عدد الكلمات المستحقة).

### 3) إصلاح مفتاح Gemini API (`js/services/gemini.js`)
- الموديل القديم `gemini-1.5-flash` كان قد أُوقف من طرف Google، فتم تحديثه إلى **`gemini-2.5-flash`** (الموديل المستقر الحالي). كل ميزات الذكاء الاصطناعي (توليد البطاقات، الترجمة، OCR، المحادثة) تعمل عليه الآن.

### 4) قسم "الإنجليزية" — مستقل بالكامل، بنفس روح Lexora
رابط جديد **"📖 الإنجليزية"** في القائمة الجانبية، صفحة كاملة مستقلة تماماً عن نظام المواد/الفلاش كاردز/المراجعات:

- **قاموس Oxford 3000 مدمج**: تم نقل نفس الـ405 كلمة من Lexora بالضبط (`js/data/oxfordWords.js`) — كل كلمة فيها مستوى (A1-B2)، ترجمة عربية، تعريف، و3 أمثلة (حياة يومية/أكاديمي/اصطلاحي).
- **مولّد دفعة يومية ديناميكي** (`js/services/englishQueue.js`): نفس منطق Lexora بالضبط — حتى 100 كلمة جديدة يومياً، بنسبة 60-70% سهل / 30-40% متوسط، بدون أي تكرار لكلمة ظهرت من قبل.
- **خوارزمية SRS بأسلوب Anki** (`js/services/englishSrs.js`): 4 أزرار تقييم (مرة أخرى / صعب / جيد / سهل) بدل زر صح/خطأ، مستقلة تماماً عن خوارزمية بطاقات المراجعة العامة.
- **تصميم مطابق لـ Lexora بالضبط**: ثيم داكن "ink & gold"، خط Fraunces الأنيق للعناوين والكلمات، بطاقة قلب ثلاثية الأبعاد (Flip Card) بنفس الحركة، شارات مستوى ملوّنة (A1 أخضر → B2 وردي).
- **3 تبويبات داخلية**: لوحة تحكم (سلسلة الأيام، إجمالي المراجعات، المستحق الآن، رصيد الكلمات) — كلمات اليوم (بطاقة القلب + التقييم) — مفرداتي (بحث + فلاتر حالة + إضافة كلمات خاصة).
- **نطق صوتي (TTS)** على كل كلمة وكل مثال، وزر **"💬 اسأل الذكاء الاصطناعي عن هذه الكلمة"** (بديل مبسّط عن AI Tutor في Lexora، يستخدم نفس مفتاح Gemini المحفوظ عندك).
- **إضافة كلمات خاصة بك** يدوياً أو بالذكاء الاصطناعي (زر "✨ تعبئة تلقائية" يملأ المستوى + الترجمة + التعريف + الأمثلة الثلاثة تلقائياً).
- بيانات هذا القسم محفوظة في مخازن IndexedDB مستقلة (`english_srs_cards` و `english_custom_words`) — لا تتقاطع إطلاقاً مع بيانات المواد/الفلاش كاردز.

### 5) حذف فلتر "الإنجليزي" القديم
- أُزيل تصنيف/فلتر "إنجليزي" بالكامل من **"المواد الدراسية"** (نافذة إضافة/تعديل البطاقة في `subject-detail.js` رجعت لشكلها البسيط الأصلي: وجه + ظهر فقط) ومن **"المراجعات"** (`reviews.js`) ومن صفحة **"بطاقات المراجعة"** (`study.js` رجع لفلتر المادة فقط بدون فلتر تصنيف).
- الإنجليزي أصبح قسمه الخاص المستقل بالكامل زي ما طلبت، بدل ما يكون مجرد تصنيف مبعثر بين الصفحات.

### ملفات جديدة كليًا في هذا التحديث
- `js/data/oxfordWords.js`
- `js/services/englishSrs.js`
- `js/services/englishQueue.js`
- `js/views/english.js`

### تنبيه مهم
- قاعدة البيانات ترقّت تلقائياً من v4 إلى v5 (تُضيف مخزنين جديدين فقط) — **بياناتك القديمة كلها محفوظة**، ولا شيء يُحذف.
- إذا كنت تستخدم ميزات الذكاء الاصطناعي في قسم الإنجليزية، تأكد إن مفتاح Gemini API محفوظ في الإعدادات (نفس المفتاح المستخدم بباقي التطبيق).

---

## تحديث ثالث — إعادة تصميم كاملة + قسم التقويم الجديد

### 1) إلغاء تصميم Lexora المنفصل بالكامل
قسم "الإنجليزية" رجع يستخدم **بالضبط نفس مكوّنات تصميم باقي أقسام مذاكر**:
- نفس بطاقات الإحصائيات (`.stat-card`) المستخدمة في لوحة التحكم.
- نفس نظام التبويبات (`.tabs` / `.tab-btn`) المستخدم في صفحة المادة.
- **نفس بطاقة القلب** (Flip Card) المستخدمة حرفيًا في "بطاقات المراجعة" — بدون أي تصميم أو ألوان أو خطوط منفصلة.
- تمت إزالة كل أثر لثيم "ink & gold" الداكن وخط Fraunces نهائيًا.

### 2) فلاتر المستوى (A1 → C2) — قابلة للضغط فعليًا
- انتقلت من لوحة التحكم إلى تبويب **"مفرداتي"**.
- كل مستوى (A1, A2, B1, B2, **C1, C2 الجديدين**) هو الآن زر فلتر حقيقي — اضغطه ليعرض كلمات هذا المستوى فقط، مع إمكانية الجمع مع فلتر الحالة (مستحقة/جديدة/قيد التعلم/مراجعة) بنفس الوقت.
- C1 وC2 جاهزان بالكامل بالنظام (بيانات، ذكاء اصطناعي، توليد الدفعة اليومية) حتى لو ما فيهم كلمات بعد.

### 3) استيراد كلمات من ملف أو صورة (`js/services/gemini.js` + `js/services/extractor.js`)
- زر جديد "📥 استيراد من ملف/صورة" في تبويب "مفرداتي" ولوحة التحكم.
- يستخدم **نفس خط أنابيب استخراج الملفات** المستخدم في "المواد الدراسية" (نص/PDF/صورة عبر OCR بالذكاء الاصطناعي).
- بعد الاستخراج، تطلع لك **قائمة كلمات بمربعات اختيار** (كلها محددة افتراضيًا) — إما تسيبها كلها (يضيف كل شيء تلقائيًا مع توليد الترجمة/التعريف/الأمثلة)، أو تلغي تحديد اللي ما تبيه وتضيف الباقي بس. الخيار بيدك بالكامل في كل مرة.

### 4) الحد اليومي صار يدوي بالكامل
- الافتراضي الآن **10 كلمات** بدل 100، وتقدر تغيّره لأي رقم من لوحة التحكم في أي وقت (بدون حد أقصى مفروض).

### 5) زر طيّ القائمة الجانبية يعمل الآن في كل الموقع
- زر الـ **X** الموجود أصلاً بأعلى القائمة الجانبية صار شغّال بكل الشاشات (كان يعمل على الجوال بس).
- اضغطه يطوي القائمة الجانبية كاملة من أي صفحة.
- يطلع **زر عائم (☰) صغير فوق يسار الشاشة** لإرجاع القائمة — يظهر بس وقت ما تكون مطوية.
- تمت إزالة زر "ملء الشاشة" المخصص لقسم الإنجليزية بالكامل (استُبدل بهذا الحل العام).

### 6) قسم "📅 التقويم" الجديد بالكامل (`js/views/calendar.js`)
تقويم شهري موحّد يجمع كل شيء له تاريخ بالتطبيق في مكان واحد:
- عرض شهري بأسلوب Google Calendar المبسّط، مع تنقّل بين الشهور وزر "اليوم".
- **الأيام القادمة** تعرض المستحق: 🃏 بطاقات مراجعة، 🗓️ نقاط مراجعة الدروس، 📖 كلمات إنجليزية مستحقة.
- **الأيام الماضية** تعرض نشاطك الفعلي: 🍅 جلسات بومودورو المكتملة، ✅ عدد المراجعات المنجزة.
- اضغط أي يوم ليفتح لك تفصيل كامل بالأسفل (الأسماء، الأعداد، وروابط مباشرة لكل قسم).
- يوم اليوم مُبرز بلون مميز، ويفتح تفاصيله تلقائيًا عند فتح الصفحة.

### ملفات جديدة في هذا التحديث
- `js/views/calendar.js`

### ملفات معاد كتابتها بالكامل
- `js/views/english.js` (إعادة تصميم شاملة)
- `css/main.css` (استبدال قسم الإنجليزية القديم بالكامل + إضافة أنماط التقويم وطيّ القائمة)
- `js/app.js` (تفعيل طيّ القائمة الجانبية + مسار التقويم)
- `index.html` (زر الطيّ العائم + رابط التقويم)
- `js/services/gemini.js` (إضافة C1/C2 + دالة استخراج الكلمات من نص)
- `js/services/englishQueue.js` (تصنيف C1/C2 ضمن "متوسط فأعلى")
